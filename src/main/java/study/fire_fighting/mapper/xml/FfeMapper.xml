<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.fire_fighting.mapper.FfeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="study.fire_fighting.pojo.Ffe">
        <id column="eid" property="eid" />
        <result column="ename" property="ename" />
        <result column="price" property="price" />
        <result column="manufactor" property="manufactor" />
        <result column="useway" property="useway" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        eid, ename, price, manufactor, useway
    </sql>

</mapper>
